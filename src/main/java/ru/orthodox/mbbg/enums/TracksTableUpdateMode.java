package ru.orthodox.mbbg.enums;

public enum TracksTableUpdateMode {
    /*
    When the table is filled on, for example, edit window opening.
     */
    RENDER,
    /*
    When user manually adds a track (either from library or from local machine)
     */
    USER
}
